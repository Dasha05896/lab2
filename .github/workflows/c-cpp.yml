name: Build Program

on:
  push:
    branches:
      - branchMake

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build program
        run: |
          g++ -o program main.cpp

